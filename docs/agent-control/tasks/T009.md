# T009 — Context Builder

## Objective
Build role-aware context from Task Contract, state, events, artifacts and relevant memory; enforce context budgets.

## Constraints
Never load full Issue history by default.

## Acceptance Criteria
Same inputs produce deterministic context; budget enforcement works; irrelevant history is excluded.

## Definition of Done
Context assembly is policy-driven and tested.
