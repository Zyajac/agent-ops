# T002 — Schemas and domain identifiers

## Objective
Implement Pydantic models and JSON schema validation for Task Contract, Event and Memory; add common IDs and enums.

## Constraints
Use `schemas/task-contract.schema.json`, `schemas/event.schema.json`, and `schemas/memory.schema.json`.

## Acceptance Criteria
Valid examples pass; malformed examples fail; schema tests pass.

## Definition of Done
Implementation, tests, and documentation are complete with no unrelated refactor.
