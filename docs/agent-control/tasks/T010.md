# T010 — Memory service

## Objective
Implement working, episodic, semantic and procedural memory with persistence, provenance and retrieval policy.

## Constraints
Start without a dedicated vector database. Do not auto-store every message.

## Acceptance Criteria
Write/read/invalidation tests pass; provenance is mandatory for persistent memory.

## Definition of Done
Memory types and write policies are explicit and tested.
